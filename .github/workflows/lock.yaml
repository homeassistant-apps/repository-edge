---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "25 3 * * *"
  workflow_dispatch:

jobs:
  workflows:
    uses: homeassistant-apps/workflows/.github/workflows/lock.yaml@main
